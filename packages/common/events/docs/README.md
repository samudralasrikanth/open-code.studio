# Event Bus

The event bus provides ordered asynchronous publish/subscribe messaging with retries, dead letters, and correlation IDs.

## Public API

- `EventBus`
- `createEventBus`
- `PlatformEvent`
